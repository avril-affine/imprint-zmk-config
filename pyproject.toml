[project]
name = "imprint-zmk-config"
version = "0.1.0"
description = "My imprint config"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "pyelftools>=0.32",
    "west>=1.5.0",
]

[tool.uv]
package = false
