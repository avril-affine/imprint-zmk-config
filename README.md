# Build

```bash
source .venv/bin/activate
./build.sh
cp latest-left.uf2 /Volumes/ASSIMILATOR
cp latest-right.uf2 /Volumes/ASSIMILATOR
```

# Notes

Double tap reset button to put into bootloader mode.
